# ======================================
# VNStock API v4.2 — Requirements
# Deploy: Render / Railway / VPS
# ======================================

# --- Web Framework ---
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.4

# --- VNStock v4 ---
vnstock==4.0.7

# --- HTTP trực tiếp (Lớp 2 fallback fund-holdings gọi thẳng fmarket.vn) ---
# Đã là dependency gián tiếp của vnstock, khai báo rõ vì main.py giờ import trực tiếp
requests

# --- DNSE OpenAPI (Market Data) ---
# Lưu ý: tên package pip là dnse-sdk-openapi (KHÔNG phải openapi-sdk như README/api.md gốc
# của DNSE ghi nhầm). Tên module để import vẫn là "dnse" (from dnse import DNSEClient) —
# tên package pip khác tên module import, giống trường hợp beautifulsoup4 -> import bs4.
# Ghim version 2.0.0 (bản mới nhất đã xác nhận trên PyPI) để tránh bản sau đổi API phá vỡ code.
dnse-sdk-openapi==2.0.0

# --- WebSocket encoding for DNSE ---
msgpack==1.2.1

# --- WebSocket client — BẮT BUỘC cho dnse-sdk-openapi ---
# dnse/__init__.py luôn import submodule dnse.websocket.client dù chỉ dùng REST Market Data,
# và submodule dnse/websocket/connection.py có "from websockets import ClientConnection".
# ĐÃ TEST THẬT (không đoán): websockets==13.1 KHÔNG có ClientConnection ở top-level, gây lỗi
# "cannot import name 'ClientConnection' from 'websockets'" ngay khi "from dnse import
# DNSEClient". Đã cài & test lần lượt 14.0 (fail), 14.1 (fail), 14.2 (OK), 15.0.1 (OK),
# 16.1.1 (OK), 17.0.1 (OK) — ClientConnection chỉ xuất hiện ở top-level từ 14.2 trở lên.
# Ghim 14.2 (bản thấp nhất đã xác nhận hoạt động) để gần nhất với môi trường SDK có thể đã
# test, giảm rủi ro thay đổi API không cần thiết ở các bản mới hơn.
websockets==14.2

# --- HTTP client for DNSE (timeout/retry config) ---
httpx==0.27.2

# --- Core Dependencies ---
# Unpinned pandas so pip uses the latest pre-compiled wheel (avoids building from source)
pandas
importlib-metadata==8.5.0
